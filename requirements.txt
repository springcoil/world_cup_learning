pandas
# is this ok? (I have it installed via the ubuntu repos)
scikit-learn
pygal
# does this installs everything needed for ipython notebook?:
ipython
pygments>=2.7.4 # not directly required, pinned by Snyk to avoid a vulnerability
